# Hadolint configuration
# https://github.com/hadolint/hadolint

ignore:
  # DL3008: Pin versions in apt-get install — acceptable for base system tools
  # in CI-built images where reproducibility is handled by pinning the base image tag
  - DL3008
  # DL3009: Delete apt-get lists — handled explicitly in our RUN layer
  - DL3009
